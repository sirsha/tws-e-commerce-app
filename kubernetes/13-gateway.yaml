apiVersion: gateway.networking.k8s.io/v1
kind: Gateway
metadata:
  name: easyshop-gateway
  namespace: easyshop
spec:
  gatewayClassName: nginx
  listeners:
  - name: http
    port: 80
    protocol: HTTP